{
  "name": "@tbsp/utils",
  "version": "0.0.0",
  "exports": {
    "./*.ts": "./*.ts",
    "./*.js": "./dist/*.js",
    "./*": "./dist/*.js"
  },
  "description": "",
  "files": [
    "*"
  ],
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx tsc --noEmit",
    "build": "npx tsc || echo 'tsc build errors' && exit 0",
    "prepare": "npm run build"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "^1.2.22"
  }
}
