{
  "name": "thebigscumproject",
  "version": "0.0.0",
  "description": "A web server and client built with sockets to implement real-time chatrooms and host games of Mafia, a popular social deception-based game.",
  "type": "module",
  "license": "ISC",
  "author": "Duskt (duskt@github.com)",
  "scripts": {
    "dev": "bun run --cwd=client build; bun run --cwd=server src/index.ts;",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:up": "docker-compose up -d --build", 
    "docker:down": "docker-compose down"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "dependencies": {
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "bun-types": "^1.2.22"
  }
}
